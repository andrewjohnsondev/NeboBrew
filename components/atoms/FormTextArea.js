import styled from 'styled-components';

const FormTextArea = styled.textarea`
  border: solid 1px hsl(var(--color-neutral-300));
  border-radius: 4px;
  padding: 0.35rem;
`;
export default FormTextArea;
