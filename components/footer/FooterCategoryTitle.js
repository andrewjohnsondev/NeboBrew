import styled from 'styled-components';

const FooterCategoryTitle = styled.h3`
  color: hsl(var(--color-white));
  font-size: var(--text-2xl);
  font-weight: var(--fw-bold);
  text-transform: uppercase;
`;

export default FooterCategoryTitle;
